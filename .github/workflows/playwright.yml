name: Playwright Tests with Allure and Deploy

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: Install dependencies
        run: npm install

      - name: Install Playwright Browsers
        run: npx playwright install --with-deps

      # Run Playwright tests and generate Allure results
      - name: Run Playwright tests with Allure
        run: npm run test:report
        continue-on-error: true

      # Install Allure CLI for report generation
      - name: Install Allure Commandline
        run: npm install -g allure-commandline --save-dev

      # List Allure results for debugging
      - name: List allure-results
        if: always()
        run: ls -R ./allure-results || echo "No allure-results found"

      # Generate Allure HTML report
      - name: Generate Allure Report
        if: always()
        run: npm run allure:generate

      # Upload Allure report as artifact for next job
      - name: Upload Allure Report Artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: allure-report
          path: ./allure-report

  deploy:
    runs-on: ubuntu-latest
    needs: test

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Download Allure Report Artifact
        uses: actions/download-artifact@v4
        with:
          name: allure-report
          path: ./allure-report

      # -------------------------------
      # Netlify Deploy
      # -------------------------------

      # Install Netlify CLI
      - name: Install Netlify CLI
        if: always()
        run: npm install -g netlify-cli

      # Check Netlify CLI version
      - name: Check Netlify version
        if: always()
        run: netlify --version

      # Deploy Allure report to Netlify (Production)
      - name: Deploy Allure report to Netlify (Production)
        if: github.ref == 'refs/heads/main'
        run: |
          netlify deploy \
            --dir=allure-report \
            --site ${{ secrets.NETLIFY_SITE_ID }} \
            --auth ${{ secrets.NETLIFY_AUTH_TOKEN }} \
            --prod
      # Add Netlify Deploy Button to GitHub summary (replace with your real Netlify URL)
      - name: Add Netlify Deploy Button
        if: github.ref == 'refs/heads/main'
        uses: actions/github-script@v7
        with:
          script: |
            const url = "https://leafy-kheer-e0f382.netlify.app"; // TODO: Replace with your Netlify site URL
            core.summary
              .addHeading("✅ Allure Report Deployed")
              .addLink("👉 Click here to view Allure Report", url)
              .addEOL()
              .write()

      # Notify Slack with the Allure report link
      - name: Notify Slack
        run: |
          curl -X POST -H 'Content-type: application/json' \
          --data "{\"text\":\"✅ Test completed! Allure Report: https://<your-site>.netlify.app/allure-report\"}" \
          ${{ secrets.SLACK_WEBHOOK_URL }}
